

<!-- Modal -->
<style scoped>
    /* Custom rules to handle the layout smoothly */
    .dropdown-menu-custom {
        max-height: 300px;
        overflow-y: auto;
        width: 100%;
        min-width: 250px;
    }

    .dropdown-item-check {
        padding: 0.5rem 1rem;
        cursor: pointer;
    }

        .dropdown-item-check:hover {
            background-color: #f8f9fa;
        }

        .dropdown-item-check input {
            cursor: pointer;
        }
    /* Hide specific search results when filtered out */
    .search-hidden {
        display: none !important;
    }

    
</style>
<!-- Button trigger modal -->
<div id="addBtnContainer" class="d-none">
    <button id="AddEquipmentOperatorBtn" type="button"  class="btn btn-danger rounded-1" data-bs-toggle="modal" data-bs-target="#AddEquipmentOperatorModal">Add New</button>
</div>

<div class="modal fade" id="AddEquipmentOperatorModal" aria-hidden="true" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-main text-white">
                <h5 class="modal-title" id="AddEquipmentOperator">Add a Certified Equipment Operator Employee</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">

                <div class="row justify-content-start  p-4">
                    <div class="col">
                        <label class="form-label">Select Employee</label>
                        <!-- Hidden real select (for form submission) -->
                        <select id="selectEquipmentOperators" class="form-select d-none">
                        </select>
                        <!-- Custom dropdown -->
                        <div class="dropdown rounded-1">
                            <button class="form-select text-start text-muted" type="button" id="chooseEquipmentOperatorEmployeeBtn" data-bs-toggle="dropdown">
                                Employees
                            </button>
                            <div class="dropdown-menu w-100 p-2" style="max-height: 350px !important; overflow-y: scroll;">

                                <!-- Search input -->
                                <input type="text" id="EquipmentOperatorSearchInput" class="form-control" placeholder="Search...">
                                <!-- Options -->
                                <ul class="list-unstyled mb-0 p-2" id="selectEquipmentOperatorEmployee">
                                </ul>

                            </div>
                        </div>

                     
                            <label class="form-label pt-2" for="operatorEquipment">Equipment</label>
                            <input class="form-control" id="operatorEquipment" />
                        
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button id="closeAddModal" type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <div id="submitBtn">
                    <button id="submitCertifiedEquipmentOperator" type="button" class="btn btn-danger">  
                        <span id="submitBtnSpinner" class="spinner-border spinner-border-sm d-none" role="status" aria-hidden="true"></span>
                        Update
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
