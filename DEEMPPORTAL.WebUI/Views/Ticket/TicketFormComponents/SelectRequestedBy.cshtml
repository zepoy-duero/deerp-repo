<div class="col-md-6 d-flex justify-content-start align-items-start">

    <label for="SelectRequestedByName" class="text-muted pe-2 text-nowrap">
        Requested By:
    </label>
    <div class="RequestedByContainer flex-grow-0">
       
        <div class="d-flex flex-row">
            <!-- Display Mode -->
            <h6 id="DisplayRequestedByName" class="fw-bold text-nowrap"></h6>
            <!-- Edit Mode -->
            @* <select id="SelectRequestedByName"
                    class="form-select form-select-sm d-none" required>
            </select>
            <i id="editBtn" onclick="showEditRequestedBy()" class="bi bi-pencil text-main extra-small ms-3 mt-1"></i>
            <i id="checkBtn" onclick="saveEditRequestedBy()" class="bi bi-floppy2 text-main ms-3 mt-1 d-none"></i> *@
        </div>
    </div>
</div>